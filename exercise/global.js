//Global object - a bit like a window object in JS inside a browser. in browser the global object is window, in node the global object is global

//console.log(global);

/*global.setTimeout(() => {
    console.log('Timeout set to 3 secs.')
    clearInterval(int);
}, 3000);*/


/*const int = setInterval( () => {
    console.log('In the interval of 1 sec.')
}, 1000);*/

//console.log('DIRECTORY PATH ', __dirname);
//console.log('FILE PATH ', __filename);

